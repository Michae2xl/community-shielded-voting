services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: zcap_voting
      POSTGRES_USER: zcap
      POSTGRES_PASSWORD: zcap
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
